{
  "name": "illustra-capture",
  "version": "0.1.0",
  "private": true,
  "description": "Illustra Capture — Chrome extension MVP for saving highlighted webpage text into a temporary Illustra capture inbox.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.278",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.0"
  }
}
